package app.entities;

public class Salesperson extends User{
    public Salesperson(int id, String name, String password, String rank, double balance) {
        super(id, name, password, rank, balance);
    }
}
